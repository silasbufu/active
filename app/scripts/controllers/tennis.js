'use strict';

/**
 * @ngdoc function
 * @name activeApp.controller:TennisCtrl
 * @description
 * # TennisCtrl
 * Controller of the activeApp
 */
angular.module('activeApp')
  .controller('TennisCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
